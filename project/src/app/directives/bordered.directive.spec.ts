import { BorderedDirective } from './bordered.directive';

describe('BorderedDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderedDirective();
    expect(directive).toBeTruthy();
  });
});
